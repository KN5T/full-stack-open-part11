name: Hello-world!

on:
  push:
    branches:
      - main
  
jobs:
  hello-wordl-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      
    
